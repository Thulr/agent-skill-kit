# Tasks — catalog consolidation

Outcome-shaped checklist. `just check` passed at each phase boundary.

## Phase ① — shared engine + routing-CSV contract
- [x] Add `skills/_shared/routing-contract.md` (documents the two router shapes + column vocab).
- [x] Add `scripts/check-routing-csv.sh` (header present, no ragged rows, all 3 lanes); wire into `Justfile`.
- [x] Make `check-release-contract.py` skip git-ignored dirs (local `just check` was broken by a gitignored `bmad*` install under `.agents/skills/`; CI unaffected).
- [x] `just check` green. (commit 085b47d)

## Phase ② — merge research (2 → 1)
- [x] `git mv` topic-research → `research/references/report/`, opportunity-research → `research/references/opportunity/`.
- [x] `frame-router.csv`, thin `SKILL.md`, per-frame `workflow.md`, merged `skill.json` (26 inspired_by), `trigger-evals` (45 queries), `activation-cases`, `run-static-checks.sh`.
- [x] Symlink fixed; OR-* finding-ID namespace preserved.
- [x] Delete old dirs; `just check` green. (commit 085b47d)

## Phase ② — merge review (7 → 1)
- [x] `git mv` 7 skills → `review-heuristics/references/<domain>/` + `templates/<domain>/`.
- [x] Rewrite 1053 skill-root-relative paths (references/→references/<domain>/, templates/→…); fix 23 symlinks; verify 0 unresolved paths.
- [x] `domain-router.csv`, thin `SKILL.md`, shared `review-workflow.md`, merged `skill.json` (122 inspired_by), `trigger-evals` (35), `activation-cases` (7 domains), `run-static-checks.sh`.
- [x] Delete 7 dirs; `just check` green. (commit c21b802)

## Phase ③ — catalog surface
- [x] README: rewrote "Which skill should I use?" (5 rows), "Ambiguous phrasings", per-skill sections (9→5), install commands.
- [x] `llms-full.txt`: updated Skills section to the 5-skill surface (+ fixed stale curator/reviewer names, word-cap note).
- [x] `llms.txt`: generic, no change needed.
- [x] AGENTS.md / CODEOWNERS: no per-skill references / path globs only → no change needed.
- [x] Cross-references in surviving skills (project-agentification, evidence-driven, loop-architect, _shared, example-minimal) updated to `review-heuristics`/`research` (+ domain).
- [x] Reflection-log entry for the falsified-cap finding.
- [x] ADR 0005 (one-engine-many-surfaces → routed, not split) + index.
- [x] `skills-lock.json`: gitignored, regenerated by the CLI → no change.
- [x] Final `just check` green.

## Deferred (out of scope; noted, not broken)
- `.agents/skills/informed-skill-curator/**` + `.claude/skills/` mirror reference the old skill names as illustrative skill-shape examples (two-level / single-layer / flat). Repo-local authoring aid, not published; the shapes still exist inside `review-heuristics` domains. Refresh in a follow-up.
- Historical records left verbatim by design: `docs/reflection-log/*`, `docs/audits/*`, `docs/research/*`, `docs/agent-readiness-2026-05-15.md`, `.agents/state/*`.
