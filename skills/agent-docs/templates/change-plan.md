# Change Plan — <what agent-native doc you are changing>

**Target persona:** <from references/core/personas.md> (usually Persona A)
**Surfaces in scope:** <agents-md | llms-txt | tool-descriptions | machine-reference | context-budget>
**Date:** <YYYY-MM-DD>

A pre-flight plan for an in-progress agent-native doc change. Fill it before writing much; it is
a thinking tool, not a deliverable to track.

## What already exists

<What agent-facing docs you have before changing — the AGENTS.md, llms.txt, tool descriptions, or
reference you will build on. Confirm AGENTS.md is hand-curated (not autogenerated) and mirrors are
synced before relying on them.>

## The minimal change

- **Reuse:** <existing index/contract/reference this builds on>
- **Add:** <the smallest agent-native doc the present need forces>
- **Remove:** <always-loaded bloat this lets you retire — answer even if "nothing">

## Agent-readability check

- **Found:** <can the agent find it — curated index, placement-by-access-pattern?>
- **Survives:** <does a retrieved chunk stand alone — local subject, stable anchor?>
- **Triggers:** <does the description disambiguate so the right tool/skill fires?>
- **Budgeted:** <always-loaded minimal; invariants in gates, not ignorable prose?>

## Seams I am deliberately NOT adding

<The bundles, mirrors, or always-loaded content you considered and are leaving out because no
present need forces them (YAGNI). Naming them documents the restraint.>

## Blast radius

<Which agents/harnesses read this. If it changes AGENTS.md or a harness mirror, note it affects
every agent on the repo; if it adds always-loaded content, note the budget cost.>

## Routing

- **Mechanical** (anchor wiring, mirror symlink, index entry): handled by <gate / locally>.
- **Judgment call to flag to a human:** <moving an invariant to a gate, a new always-loaded file,
  an AGENTS.md rule promotion — or "none">.

## Grounding sources applied

- <skill.json inspired_by entry> — <how it shaped this plan>
