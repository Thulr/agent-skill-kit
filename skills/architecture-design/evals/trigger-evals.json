{
  "skill": "architecture-design",
  "version": "0.1.0",
  "queries": [
    {"query": "we're starting a new go service with two bounded contexts and an unclear seam between them. help me decide where the boundary should go before we write any code", "should_activate": true, "expected_route": "design/bounded-context", "category": "positive"},
    {"query": "designing a new feature module — how should i set up the layers, ports, and adapters so the domain doesn't depend on the framework?", "should_activate": true, "expected_route": "design/boundaries", "category": "positive"},
    {"query": "i'm shaping the domain for a new fulfilment service. what should be an entity vs a value object, and where do the aggregate boundaries go?", "should_activate": true, "expected_route": "design/domain-model", "category": "positive"},
    {"query": "set the dependency-direction invariant for this new payments module up front — which way should source-code dependencies point and how do we enforce it?", "should_activate": true, "expected_route": "design/dependency-rule", "category": "positive"},
    {"query": "our handlers call the database directly. plan a sequenced, reversible refactor to introduce ports and adapters without a big-bang rewrite", "should_activate": true, "expected_route": "refactor/boundaries", "category": "positive"},
    {"query": "we have a god Order entity doing everything. plan how to split the aggregate and pull value objects out, step by step so each step is safe", "should_activate": true, "expected_route": "refactor/domain-model", "category": "positive"},
    {"query": "plan a strangler-fig extraction of the billing context out of our monolith — what's the safe sequence with branch-by-abstraction?", "should_activate": true, "expected_route": "refactor/bounded-context", "category": "positive"},
    {"query": "the team is new to the codebase — explain ports-and-adapters and the dependency rule against our actual structure, not a textbook", "should_activate": true, "expected_route": "explain/boundaries", "category": "positive"},
    {"query": "explain the difference between an aggregate, an entity, and a value object, and what 'anemic domain model' actually means", "should_activate": true, "expected_route": "explain/domain-model", "category": "positive"},
    {"query": "our spring monolith has anemic entities and fat service classes. audit whether our domain is anemic and score how bad it is", "should_activate": false, "expected_route": null, "category": "negative"},
    {"query": "audit the dependency direction across our modules — i think use cases import the JPA repository implementations directly", "should_activate": false, "expected_route": null, "category": "negative"},
    {"query": "verify whether CA-DEP-002 is fixed using docs/audits/architecture-audit-workflow-state-2026-05-22-billing.json and this PR diff", "should_activate": false, "expected_route": null, "category": "negative"},
    {"query": "we're shipping a 1.0 of a public SDK next quarter — what api contracts and surface decisions should i pin down before we write code so we don't get a hyrum's-law nightmare?", "should_activate": false, "expected_route": null, "category": "negative"},
    {"query": "design a lower-friction consumer signup flow — 3 steps instead of 4, here's the current form", "should_activate": false, "expected_route": null, "category": "negative"},
    {"query": "design the agent loop and tool-call contract for a python AI/Agent SDK with stopWhen-style predicates and pre-tool approval hooks", "should_activate": false, "expected_route": null, "category": "negative"},
    {"query": "refactor this typescript function to use early returns instead of nested if statements", "should_activate": false, "expected_route": null, "category": "negative"}
  ]
}
