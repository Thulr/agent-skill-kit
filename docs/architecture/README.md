# Architecture

This directory is for durable reference docs about repository structure and
constraints (repo maps, conventions, “where does X live?”).

Current entrypoints:

- `llms.txt` / `llms-full.txt` at repo root — curated index for agents
- `AGENTS.md` — short instruction surface pointing into `docs/`

