# Release Notes

The team wants to swap the model behind the production assistant from the
current model to a newer model. The assistant spans support, billing triage,
knowledge-base lookup, and account-status tool calls.

The release owner needs to know whether the new model is safe to ship. The
team wants a fixed task suite, baseline comparison, cost and latency thresholds,
and a rollback decision before broad rollout.
